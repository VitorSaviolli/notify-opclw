services:
  bot:
    build: .
    container_name: notificador-bot
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    network_mode: host
